---
import Default from '@astrojs/starlight/components/TableOfContents.astro';

const route = Astro.locals.starlightRoute;

if (route.toc?.items?.[0]?.slug === '_top') {
  route.toc = {
    ...route.toc,
    items: route.toc.items.slice(1),
  };
}
---
<Default><slot /></Default>
